{
	email admin@example.com
}

# 对外的推理入口 —— 指向 LiteLLM。
#
# 用户拿到的虚拟 key 在这里校验，身份由 LiteLLM 从 key 的元数据解析后
# 注入下游，不采信客户端自带的身份头。Proxy 作为内部服务不在此暴露。
api.{$PUBLIC_HOST} {
	reverse_proxy litellm:4000
}

# 运维控制台：账号、账号池、请求统计、错误诊断
console.{$PUBLIC_HOST} {
	reverse_proxy console:7004
}
